# Decorix Documentation

The canonical monorepo overview is the root [README.md](../README.md).

Package-specific usage notes live in `packages/*/README.md`. This directory is reserved for longer guides that should not duplicate the root README.

